<Project>
  <!-- Merge with parent Directory.Build.props -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <Import Project="$(SysInformRoot)/Common.User.props" />
</Project>
